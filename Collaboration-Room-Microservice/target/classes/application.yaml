spring:
  application:
    name: COLLABORATION-ROOM-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/collaborationRoom-db?createDatabaseIfNotExist=true
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  cloud:
    config:
      enabled: false
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 49fdc24b52e3fe
    password: c25c56a842cd66
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  frontend:
    url: http://localhost:4200
  reset-token:
    expiry-minutes: 15

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    hostname: localhost
    instance-id: ${spring.application.name}:${server.port}

jwt:
  secret: YourSecretKeyForJWTTokenGenerationShouldBeAtLeast256BitsLongForHS256
  expiration-ms: 86400000
